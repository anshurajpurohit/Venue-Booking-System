// script.js

document.addEventListener('DOMContentLoaded', function () {
    // Example: Alert when a user clicks on 'Manage Users'
    const manageUsersLink = document.querySelector('.nav-link[href="manage_users.php"]');
    
    manageUsersLink.addEventListener('click', function (event) {
        alert('You are navigating to Manage Users!');
    });

    // Example: Adding functionality to dynamically load data (if needed)
    function fetchData() {
        // Simulate fetching data and update DOM elements
        console.log("Fetching data for admin dashboard...");
        // For example, you could update a card's content dynamically
    }

    // Example call to fetch data when page loads
    fetchData();
});
